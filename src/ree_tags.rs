// Ree tag protection functions were removed when the oxfmt pipeline was replaced
// by the custom AST parser (ree_parser). This module is kept as a stub so that
// main.rs `mod ree_tags;` still compiles.
